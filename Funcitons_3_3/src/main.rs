fn main() {
    println!("Hello, world!");
}

fn another_function(x: i32) -> i32 {
    // Statements
    let y = 6;

    // Expressions == 함수
    {
        let x = 3;
        x + 1
    };

    // return 문을 사용하거나 ;를 붙이지 않는 마지막 표현식이 함수의 반환값임
    return 5;
    6
}
